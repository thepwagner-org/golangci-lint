name: Build
on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: read
  packages: write
  pull-requests: write
  id-token: write

jobs:
  build:
    uses: thepwagner-org/actions/.github/workflows/build.yaml@552d802cd0888d34410e1c4731e3ee559a18c5c2 # tag=v2.2.2
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


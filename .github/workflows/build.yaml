name: Build
on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write 
  actions: write
  pull-requests: write

jobs:
  build:
    uses: thepwagner-org/actions/.github/workflows/build.yaml@e0ff6950a1131aa68785ae9d04ae402b961608eb # tag=v1.0.9
    secrets:
      not_github_token: ${{secrets.WAPWAGNER_TOKEN}}

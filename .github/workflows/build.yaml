name: Build
on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write 
  actions: write
  pull-requests: write

jobs:
  build:
    uses: thepwagner-org/actions/.github/workflows/build.yaml@53b35848abf0fbd06d00211b3444d9f222df22c5 # tag=v1.0.11
    secrets:
      not_github_token: ${{secrets.WAPWAGNER_TOKEN}}

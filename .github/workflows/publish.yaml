name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@004a11d0b52f514d9907b4c8ddfd814414bb316e # v2.4.0
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


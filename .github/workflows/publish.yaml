name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@2d8703b6d944155b82b5b3031305479e0375ea77 # tag=v2.0.2
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


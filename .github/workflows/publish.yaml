name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@9e562e3e6a29fafcfd3e0ad1c21e3db9146fde48 # tag=v1.0.8
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}

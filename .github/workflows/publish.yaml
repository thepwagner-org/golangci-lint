name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@2a16746b8008eac282ed6d7ac824388c668ea6ba # tag=v2.1.1
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


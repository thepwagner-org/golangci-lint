name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@05de59550ccea9b58da28fd918fc5a0573328639 # v2.10.0
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


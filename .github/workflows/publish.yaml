name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@456495897f1124879a3700ef25e55673ae689fad # tag=v1.0.5
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}

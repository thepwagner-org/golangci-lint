name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@53b35848abf0fbd06d00211b3444d9f222df22c5 # tag=v1.0.11
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}

name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@bc480c6823a6a6a425217a1f75b581552f2a600d # tag=v2.1.0
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


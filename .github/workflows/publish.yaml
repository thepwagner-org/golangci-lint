name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@b7550187b35a45c5774d6f691d4cb0cdc62052cf # tag=v1.0.4
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}

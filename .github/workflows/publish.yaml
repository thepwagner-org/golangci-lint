name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@7a8c48d526143c8b058cd97476f7fcea16deb674 # v2.9.0
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@4205107d9049b7946fb0276c564a35afb9804d81 # tag=v1.0.6
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}

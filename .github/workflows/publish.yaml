name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@3d2e2bbbfb76e15d51348dae21130662658d5e36 # tag=v1.0.12
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}

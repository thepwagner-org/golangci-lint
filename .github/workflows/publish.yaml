name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@9dabd0167c407847daf71746b9ba09a3bedcb357 # tag=v2.0.1
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}


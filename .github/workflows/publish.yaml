name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@552d802cd0888d34410e1c4731e3ee559a18c5c2 # tag=v2.2.2
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}

